pre-commit:
  parallel: true
  commands:
    lint:
      glob: "*.{js,jsx,ts,tsx}"
      run: yarn eslint {staged_files}
    types:
      glob: "*.{ts,tsx}"
      run: yarn run tsc -p tsconfig.json --noEmit
commit-msg:
  parallel: true
  commands:
    commitlint:
      run: yarn commitlint --edit